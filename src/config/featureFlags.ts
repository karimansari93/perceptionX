/**
 * Feature flags for dashboard. Set to false to unlock after production deploy.
 * When true: add company, add location, and add prompt are disabled on the dashboard (admin unchanged).
 */
export const DASHBOARD_ADD_LOCKED = true;
